# Copy this file to backend/.env and fill with real secrets (DO NOT COMMIT)
HF_TOKEN="<YOUR_HUGGINGFACE_TOKEN>"
REPLICATE_API_TOKEN="<OPTIONAL_REPLICATE_TOKEN>"

# Example:
# HF_TOKEN="hf_xxx"

# After creating .env, ensure backend/.env is in .gitignore
